# 古诗抽查 PWA — 修改思路文档

> 生成时间：2025-08-16
> 用途：为下一个 Agent 提供完整上下文，包含当前架构问题、已确认的修改方向和具体实施步骤

---

## 一、当前架构问题

### 1. 两层架构导致手势接力是脆弱的 hack

**现状：** `PoemCardPage.vue` 采用「浏览层 + 详情层」两层架构：
- **浏览层**：CardSwiper（coverflow）+ MysteryBox
- **详情层**：RecitationCard（绝对定位覆盖在浏览层之上）
- 两层之间通过 `useSwipeHandoff` 做手势接力：从详情层拖拽超过阈值后，将 Swiper 内部状态（touches、touchEventsData）直接 hack 设置，让 Swiper 接管手势

**问题：**
- `useSwipeHandoff.ts` 直接操作 Swiper 内部私有属性（`touches.startX`、`touchEventsData.isMoved` 等），这在 Swiper 版本更新时极易失效
- 手势接力需要处理 `dragPhase` 的 5 个状态（idle → pending → dragging → transitioning → handed-off），逻辑复杂
- 详情层的 `detailLayerStyle` 需要手动计算 scale/opacity/transition，跟手感和动画都需手动调校
- 桌面端需要额外处理 Pointer 事件（`onDetailPointerDown/Move/End`），和 Touch 事件逻辑重复

### 2. 盲盒状态容易丢失

**现状：** `MysteryBox` 用 `v-show` 保持状态，但 `fromMystery` 标记和 `mysteryRevealedPoems` 在详情层和浏览层切换时可能被重置。

**已修复的 bug：** 回到盲盒页面后没有使用之前盲盒状态（commit `15dfb45`），但根本问题（两层架构下的状态同步）仍然存在。

### 3. PoemCard 展开逻辑冗余

**现状：** `PoemCard.vue` 有展开/收起状态，点击卡片先展开再进入详情。但实际上用户点击卡片就直接进入详情层了，展开状态几乎看不到。

---

## 二、已确认的修改方向

### 核心决策：合并为单一 Swiper 架构

**用户原话：** "详情页面和滑动页面就应该是同一个页面。滑动只是详情页面的一个功能。在同一个页面里面，这个逻辑应该会比较好做。"

**目标架构：** 详情层和浏览层合并为同一个 Swiper，每个 slide 直接放 RecitationCard（而不是 PoemCard），滑动即切诗。

### viewMode 三种状态

| 模式 | Swiper effect | slide 宽度 | 说明 |
|------|--------------|-----------|------|
| `swiper`（浏览） | `coverflow` | 65% | 卡片封面效果，点击进入背诵 |
| `recite`（背诵） | `slide` | 100% | 全宽展示，逐句标记 |
| `mystery`（盲盒） | 不使用 Swiper | — | 保持现有盲盒 UI |

### 需要删除的代码

- `useSwipeHandoff.ts` — 不再需要 hack Swiper 内部状态
- `useSwipeHandoff` 的所有引用和 `dragPhase`/`dragDeltaX` 相关逻辑
- `onDetailTouchStart/Move/End`、`onDetailPointerDown/Move/End` — 不再需要手动拖拽
- `detailLayerStyle` 计算属性 — 不再需要详情层动画
- 详情层 DOM（`v-if="viewLayer === 'detail'"` 的整个 div）— 不再需要
- `PoemCard.vue` 的展开/收起逻辑 — 简化为纯展示

---

## 三、具体实施步骤

### Step 1: 修改 PoemCardPage.vue — 移除两层架构

**改动点：**
1. 删除 `viewLayer` 状态（`browse`/`detail`），只保留 `viewMode`（`swiper`/`recite`/`mystery`）
2. 删除 `detailEnterAnim`、`dragPhase`、`dragDeltaX`、`dragStartX/Y`、`dragDirection` 等所有拖拽相关状态
3. 删除 `useSwipeHandoff` 引用
4. 删除所有 `onDetailTouch*`、`onDetailPointer*` 事件处理函数
5. 删除 `detailLayerStyle` 计算属性
6. 删除模板中的详情层 `<div v-if="viewLayer === 'detail'">` 及其内容
7. 删除模板中的详情层相关事件绑定（`@touchstart`、`@touchmove` 等）

**替代方案：**
- `viewMode === 'swiper'` 时：Swiper + PoemCard（封面效果）
- `viewMode === 'recite'` 时：Swiper + RecitationCard（全宽 slide）
- 点击 PoemCard → 切换 `viewMode` 到 `recite`，同时 Swiper 切换 effect 为 `slide`
- 左右滑动 → Swiper 自带处理，不需要任何 hack

### Step 2: 修改 CardSwiper.vue — 支持动态切换 effect

**改动点：**
1. 新增 prop：`effect`（`'coverflow' | 'slide'`），默认 `'coverflow'`
2. 新增 prop：`slideWidth`（`'65%' | '100%'`），默认 `'65%'`
3. 当 `effect` 变化时，更新 Swiper 参数并重新初始化（或使用 `swiper.params.effect` 动态修改）
4. 切换到 recite 模式时：`effect='slide'`、`slideWidth='100%'`、`freeMode` 可关闭
5. 切换到 swiper 模式时：`effect='coverflow'`、`slideWidth='65%'`、`freeMode` 开启

**注意：** Swiper 不支持动态切换 effect，需要 destroy + 重新初始化。用 `v-if` 的 key 变化来触发重建是最简单的方式。

### Step 3: 修改 PoemCard.vue — 简化

**改动点：**
1. 删除 `expanded` 状态和 `toggleExpand` 函数
2. 删除 `watch` poem.id 的重置逻辑
3. 卡片只显示标题 + 作者/朝代（之前展开状态的内容）
4. 点击直接 emit `click` 事件，不做展开动画

### Step 4: 处理盲盒模式

**当前行为：** 盲盒模式下点击已开盒 → 进入详情层 → 只在已开盒诗之间切换

**新架构下：**
- 盲盒模式保持不变（`viewMode === 'mystery'`）
- 点击已开盒 → 切换到 `viewMode === 'recite'`，Swiper 的数据源为 `mysteryRevealedPoems`
- `fromMystery` 标记保留，用于控制 Swiper 的数据源和"返回盲盒"按钮
- Swiper 切换到 recite 模式时，初始 slide 对齐到点击的盲盒诗

### Step 5: 处理进度条和导航

**浏览模式（swiper）：**
- 保留现有进度条（`currentIndex + 1 / poems.length`）
- 保留底部"点击卡片进入详情"提示

**背诵模式（recite）：**
- 顶部显示 `X/Y` 进度 + 返回按钮
- 底部 RecitationCard 的"上一首/下一首"按钮保留
- 左右滑动切换诗（Swiper 原生支持）
- 提交结果后自动 `slideNext()`

### Step 6: 删除 useSwipeHandoff.ts

确认不再使用后删除文件。

---

## 四、关键文件清单

| 文件 | 操作 | 说明 |
|------|------|------|
| `src/views/PoemCardPage.vue` | **大改** | 移除两层架构，合并为单一 Swiper |
| `src/components/CardSwiper.vue` | **中改** | 支持动态 effect 和 slideWidth |
| `src/components/PoemCard.vue` | **小改** | 删除展开逻辑，简化为纯展示 |
| `src/composables/useSwipeHandoff.ts` | **删除** | 不再需要 |
| `src/components/RecitationCard.vue` | 不变 | — |
| `src/components/MysteryBox.vue` | 不变 | — |

---

## 五、数据流设计

### 当前数据流（两层架构）
```
PoemCardPage
├── viewLayer: 'browse' | 'detail'
├── viewMode: 'swiper' | 'mystery'
├── currentPoem: Poem | null
├── currentIndex: number
├── 浏览层
│   ├── CardSwiper → PoemCard (点击 → enterDetail)
│   └── MysteryBox (点击 → onMysterySelectAndEnter)
└── 详情层 (v-if="viewLayer === 'detail'")
    └── RecitationCard (拖拽 → handOffToSwiper → 浏览层)
```

### 新数据流（单一 Swiper）
```
PoemCardPage
├── viewMode: 'swiper' | 'recite' | 'mystery'
├── currentIndex: number
├── currentPoem: computed from poems[currentIndex]
├── swiperMode (浏览)
│   └── CardSwiper(effect=coverflow, slideWidth=65%) → PoemCard
│       └── 点击 → viewMode = 'recite'
├── reciteMode (背诵)
│   └── CardSwiper(effect=slide, slideWidth=100%) → RecitationCard
│       └── 左右滑动 → Swiper 原生切诗
│       └── 提交 → slideNext() 或 viewMode = 'swiper'
└── mysteryMode (盲盒)
    └── MysteryBox (点击 → viewMode = 'recite')
```

---

## 六、Swiper effect 切换方案

Swiper 不支持运行时动态切换 effect，需要销毁重建。推荐方案：

**方案 A：`v-if` + key 变化触发重建**
```vue
<CardSwiper
  v-if="viewMode === 'swiper' || viewMode === 'recite'"
  :key="viewMode"
  :effect="viewMode === 'recite' ? 'slide' : 'coverflow'"
  :slide-width="viewMode === 'recite' ? '100%' : '65%'"
  v-model="currentIndex"
  :count="poems.length"
>
  <SwiperSlide v-for="(poem, index) in poems" :key="poem.id">
    <RecitationCard v-if="viewMode === 'recite'" :poem="poem" ... />
    <PoemCard v-else :poem="poem" ... @click="viewMode = 'recite'" />
  </SwiperSlide>
</CardSwiper>
```

**注意：** key 变化会销毁旧 Swiper 并创建新 Swiper，`currentIndex` 通过 v-model 传入，新 Swiper 的 `initialSlide` 会自动对齐。

---

## 七、测试要点

### 单元测试
- `useSwipeHandoff.ts` 删除后，相关测试也应删除
- `PoemCardPage` 的逻辑变更需验证：
  - `viewMode` 切换正确
  - `fromMystery` 状态保留
  - 进度计算正确

### E2E 测试
- 点击 PoemCard → 进入背诵模式
- 背诵模式左右滑动 → 切换诗
- 提交结果 → 自动下一首
- 最后一首提交 → 返回浏览模式
- 盲盒模式 → 点击已开盒 → 进入背诵模式
- 背诵模式 → 返回盲盒 → 盲盒状态保留
- 从盲盒切到全局古诗 → 数据源切换

---

## 八、潜在风险

1. **Swiper 重建时的闪烁**：`v-if` + key 变化会导致短暂空白，需要用 CSS transition 或淡入动画遮盖
2. **RecitationCard 状态重置**：Swiper 重建时，RecitationCard 的逐句标记状态会丢失。解决方案：在 `onDetailSubmit` 之前用户不应切换诗，所以实际影响不大
3. **盲盒模式下的 Swiper 数据源**：`fromMystery` 时 Swiper 应只显示 `mysteryRevealedPoems`，切换到全局时改为 `allPoems`。需要确保 `currentIndex` 正确对齐
4. **freeMode 在背诵模式下**：背诵模式可能需要关闭 freeMode，使用普通 snap 滑动，避免过度滑动跳过很多诗

---

## 九、记忆中的关键反馈

1. **不需要长按**：进入详情用点击卡片，返回浏览用左右拖拽（或滑动）。长按太难触发，体验差。
2. **无缝滑动**：用户期望在详情页左右滑能直接切到下一首诗，不需要先回到浏览层再切。
3. **盲盒状态保留**：从盲盒进入详情再返回，盲盒状态不应被重置。
